<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/inform_dialog_width"
    android:layout_height="@dimen/inform_dialog_height"
    android:layout_gravity="center_horizontal"
    android:background="@drawable/bg_dialog_survey_list_style"
    android:padding="@dimen/popup_name_record_padding" >

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/name_for_record"
        android:textColor="#666666"
        android:textSize="15sp" />

    <EditText
        android:id="@+id/et_name"
        android:layout_width="match_parent"
        android:layout_height="@dimen/popup_name_record_edit_height"
        android:layout_centerInParent="true"
        android:background="@drawable/bg_record_name"
        android:gravity="left|center"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:textColor="#333333" />

    <TextView
        android:id="@+id/tv_save"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dialog_button_height"
        android:layout_alignParentBottom="true"
        android:background="#0071bc"
        android:gravity="center"
        android:text="@string/save"
        android:textColor="@color/white"
        android:textSize="15sp" />

</RelativeLayout>